# Задача: импорт статей и новостей из MODX

Обновлено: 2026-09-17 22:22:46 +03:00.
Область: backend-импорт и media-volume, модели/роуты контента, публичные карточки контента/людей/команд/шоу, локальная MongoDB.
Основание: ветка `codex/restore-articles-news-media`, база `origin/main` @ `278bf8d525cbedc577e2473ac807bf9d3ff1e859`.
Статус: выполнено, закоммичено и проверено; готово к PR.

## Результат и границы

Восстановить из `backups/idemsku8_modx2.sql` все опубликованные статьи и новости в формате текущей админки и публичного сайта,
с повторяемым импортом, старыми URL и проверяемыми связями. Черновики и комментарии не входят в этот этап.

## Решения и вопросы

- Согласовано по данным: перенесены только опубликованные ресурсы — 66 статей и 1008 новостей.
- Решение: `createdby` не используется как публичный автор, потому что это технический пользователь MODX и старый сайт не показывал подпись автора.
- Открыто: перенос 1054 старых комментариев требует решения по учётным записям, приватности и цепочкам ответов.
- Открыто: 4 старых внутренних адреса пока не имеют целевых страниц.

## Состояние

- Выполнено: `backend/scripts/import_articles_news_modx.py`, преобразователь `backend/services/modx_articles_news.py`, индексы `old_id`, исправление редиректа новостей и поддержки `image`.
- Выполнено: в MongoDB 66/66 статей и 1008/1008 новостей имеют `old_id`, `old_urls` и контент; дублей `old_id` нет.
- Выполнено: восстановлены все 25 отсутствовавших изображений статей и новостей; `restore_article_media_modx.py --kind all` показывает 0 отсутствующих файлов, 24 исправленных URL статей отдают `200 image/*`.
- Выполнено: карточки на `/people`, `/kvn/teams`, `/shows` вписывают изображения через `object-contain`; визуально проверены все три списка и статья с восстановленными фото.
- Выполнено: до записи создан `backups/humorpedia_backup_20260917_before_articles_news.archive.gz`, SHA-256 `AE106050995ECBEF1EDF4E7EBE56264704649B3389DC466BC4F76975E8E0DA59`; dry-run восстановления прошёл.
- Не переносилось: черновики статей 2634/2963, черновик новости 3750, старые комментарии.
- Чужие изменения: рабочее дерево до задачи было чистым.

## Проверки

- `pytest -q` — 212 passed, 4 прежних deprecation warnings.
- `yarn build` — успешно; остались прежние lint warnings в несвязанных компонентах.
- Полный dry-run `import_articles_news_modx.py --all` — 1074 ресурса, 0 ошибок; 25 отсутствующих файлов, 4 неразрешённых адреса.
- После перезапуска: `/api/health` healthy, `/api/content/articles` total 66, `/api/content/news` total 1008, старый URL новости разрешается в `/news/news-00001`, frontend отвечает HTTP 200.

## Продолжение

Если комментарии нужны на новом сайте, сначала определить политику сопоставления старых авторов с `users`, видимость персональных данных
и сохранение веток ответов. Для четырёх неразрешённых старых ссылок нужны соответствующие целевые разделы либо явные редиректы.
