# Задача: проекты участников команды КВН

Обновлено: 2026-09-19 20:50:05 +03:00.
Область: `backend/services|routes/show_appearances.py`, публичная страница команды, импорт команд КВН.
Основание: ветка `codex/team-projects-auto-manual`, HEAD до незакоммиченных изменений `8bb61715815248863d4e63d96b15b19119d81cf4`.
Статус: выполнено локально.

## Результат и границы

Страница команды КВН строит раздел «Участие членов команды в других проектах» обратным
проходом `memberships.person_id → show_appearances`. Ручной проектный `text_block` сохраняется
как редакционное дополнение в той же карточке. Команды шоу этот агрегат не используют.

## Решения и вопросы

- Согласовано: автоматические данные не удаляют и не перезаписывают ручной текст.
- Согласовано: старый заголовок публично заменяется на «Участие членов команды в других проектах».
- Согласовано: точная временная фильтрация не выполняется — у `show_appearances` нет календарных годов.
- Согласовано: автоматический блок учитывает только индивидуальные участия без `team_id`;
  командные участия остаются в отдельном блоке «Команда в других шоу».
- Согласовано: автоматический и ручной блоки разделены только горизонтальной линией, без
  подзаголовков; участники одного проекта перечислены одной строкой без ссылок на страницы людей.
- Граница: полнота зависит от подтверждённых `person_id` состава и текущего набора `SHOW_PATHS`.

## Состояние

- Выполнено: пакетный публичный API `GET /api/show-appearances/teams/{id_or_slug}/projects`.
- Выполнено: единый `TeamProjects` для индивидуальных записей и ручного HTML; один пункт оглавления.
- Выполнено: импорт сохраняет ручной блок под новым каноническим заголовком.
- Выполнено: обновлены `SHOW_APPEARANCES.md`, `API.md`, `DATA_MODEL.md`, `FRONTEND.md`, `CLAUDE.md`.

## Проверки

- `docker compose exec -T backend pytest -q tests` — 344 passed, 4 warnings.
- `docker compose exec -T frontend yarn test --watchAll=false --runInBand` — 156 passed.
- `docker compose exec -T frontend yarn build` — успешно, только ранее известные ESLint warnings.
- `GET /api/show-appearances/teams/25/projects` — 200, только индивидуальные записи;
  участники сгруппированы по проекту.
- `http://localhost:3000/kvn/teams/25` — подзаголовков и ссылок на людей нет, ручной
  блок отделён линией, два участника Stand Up показаны одной строкой; console errors отсутствуют.

## Продолжение

После новых данных состава или шоу пересчитать `show_appearances`; качество автоматического
раздела исправлять в исходном membership/appearance, а не новым дублирующим текстом.
