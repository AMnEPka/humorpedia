# Задача: автоматический блок свежих новостей

Обновлено: 2026-09-19 07:54 +03:00.
Область: backend related-news API/миграция, public detail-страницы, admin-настройки и редактор новости.
Основание: ветка `codex/related-news`, база `origin/main@1d7d1b239688ea57cc6105e7a3c60169799dd6d0`.
Статус: завершено, изменения закоммичены, открыт PR #41.

## Результат и границы

- На людях, командах КВН, командах шоу и шоу после первого контентного блока показываются до трёх свежих явно связанных новостей.
- Admin управляет глобальным включением, четырьмя областями, сроком свежести и лимитом 1–3.
- Старые текстовые секции «Новости» и `humor_chronicles` удаляются отдельной идемпотентной миграцией.
- Для локальной визуальной проверки создаются 10 явно маркированных демо-новостей; скрипт поддерживает точечную очистку.

## Решения и вопросы

- Согласовано: все четыре области включены по умолчанию; лимит по умолчанию и максимум — 3; свежесть — 183 дня.
- Согласовано: `humor_chronicles` удаляется полностью.
- Согласовано: демонстрационные новости создаются только в локальной БД и помечаются `demo_marker=related_news_demo`.

## Состояние

- Выполнено: backend API/settings, frontend-блок/admin UI, явные связи новости с командами и шоу.
- Выполнено: локально применена миграция — 147 секций, 416 хроник, 215 перенесённых связей, 0 неразрешённых ссылок; повторный dry-run — 0 изменений.
- Выполнено: локально создано 10 демо-новостей о существующих людях.
- Выполнено: полная backend/frontend проверка, синхронизация контракта, документация и ручной UI-smoke публичной страницы и admin settings.

## Проверки

- `pytest -q tests/test_related_news.py tests/test_auth_guards.py tests/test_modx_articles_news.py` — 121 passed, 4 warnings.
- Полный backend suite после rebase — 318 passed, 4 warnings.
- Полный frontend suite — 11 suites, 140 tests passed.
- Production build — успешно; только существующие ESLint/Browserslist warnings.
- `node scripts/sync-module-contract.cjs --check` и `git diff --check` — успешно.
- `python scripts/migrate_related_news.py` после apply — 0 изменений.
- Ручной UI-smoke: блок «Свежие новости» расположен после первого контентного блока; admin settings показывает глобальный выключатель, срок, лимит и четыре независимые области.

## Продолжение

При необходимости удалить локальные демо-новости командой `python scripts/seed_related_news_demo.py --cleanup --apply`; после review — влить PR в `main`.
